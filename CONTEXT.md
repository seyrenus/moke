# Moke

Moke 是原生 Android SSH/mosh 终端客户端。本文件是项目统一语言的词汇表：给概念定名、划清边界。

## Language

### 文件

**文件页 (Files)**:
浏览某台主机远端目录树的页面，经独立 SFTP 连接列目录、收发文件。从属于主机或终端会话，不是全局分区。

**文件查看器 (File Viewer)**:
应用内只读呈现一个远端文件内容的页面，从文件页点文件进入；呈现方式由查看器决定（文本渲染、二进制不可读提示）。
_Avoid_: 预览（已被外观页的实时终端效果占用）、文件编辑器（只读，不能编辑）

**Diff 查看器 (Diff Viewer)**:
以 +/- 着色呈现 git 改动的查看器，是文件查看功能族的第二种内容类型，与文件查看器共用取内容、滚动等基础设施。入口在文件页（⋮ 的 repo 级、文件长按的单文件级）。
_Avoid_: git 视图、版本对比（不做任意两个版本间的比较）

**未提交改动 (Uncommitted changes)**:
工作区相对 HEAD 的全部差异：已暂存 + 未暂存 + 未跟踪文件（以全新增呈现）。Diff 查看器的固定范围语义。
_Avoid_: 与 `git diff` 命令的输出范围（仅未暂存）混用——命令只是数据源，不是语义

## Example dialogue

> 开发者：用户在文件页长按 `main.py` 想看它改了什么，但它是新文件，`git diff` 里没有它。
> 领域：那就进 Diff 查看器看未提交改动——未跟踪文件属于未提交改动，呈现为全新增，而不是「没有改动」。
> 开发者：这和 `git diff` 的输出不一致吧？
> 领域：`git diff` 只是数据源命令之一；未提交改动还包括已暂存和未跟踪。语义不跟着命令的输出范围裁剪。

## Flagged ambiguities

- **预览**：代码中 `PreviewTransport` 的「预览」指外观页的实时终端效果，与查看远端文件无关。文件内容查看一律称「查看器」。
